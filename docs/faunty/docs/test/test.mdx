---
title: MDX Playground — Test
description: "Demonstrates MDX features: admonitions, tabs, callouts, code blocks, and interactive examples."
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# MDX Playground

:::note
This MDX file is a playground demonstrating available MDX features for the Faunty docs site. Use it as a template when creating new documentation pages.
:::

:::tip
You can embed React components, import theme components like `Tabs`, and use admonitions (`:::note`, `:::info`, `:::danger`).
:::

## Notices and Admonitions

:::info
**Info:** Use `:::info` for supplementary information that helps but isn't critical.
:::

:::warning
**Warning:** Use `:::warning` for important cautions (breaking changes, deprecated APIs).
:::

:::danger
**Danger:** Use `:::danger` for irreversible actions or security notices.
:::

## Tabs

<Tabs>
  <TabItem value="md" label="Markdown Example">

```md
### This is a markdown sample

- Use lists
- Use code blocks
```

  </TabItem>
  <TabItem value="code" label="Code Example">

```dart
// Example Flutter widget
class ExampleWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Hello Faunty');
  }
}
```

  </TabItem>
</Tabs>

## Details / Expand

<details>
  <summary>Click to reveal contributor checklist</summary>

- [ ] Fill template
- [ ] Add Figma links
- [ ] Reference `lib/components/`

</details>

## Interactive React snippet (Docusaurus)

```jsx
export default function PlaygroundButton() {
  return (
    <button onClick={() => alert('This is a demo')}>Click me</button>
  );
}
```

## Code block with language and copy button

```bash
# Example deploy command (PowerShell)
.
```

:::caution
This is a demo file. When copying blocks into real docs, replace placeholder paths and links with repository-specific ones (e.g., `lib/components/`).
:::

---

If you want additional interactive widgets (live code execution, embedded Figma frames, or Storybook previews), I can add examples for that platform.
